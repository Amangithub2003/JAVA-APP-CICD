apiVersion: v1
kind: Service
metadata:
  name: java-app-service
spec:
  type: NodePort
  selector:
    app: java-app
  ports:
    - protocol: TCP
      port: 8081        # Service port
      targetPort: 8081  # Match container port
      nodePort: 30400   # <-- free port
